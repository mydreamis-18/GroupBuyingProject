const init = {
  //
  currentProductId: "",
  defaultImg: true,
  product: {},
};
function reducer(state = init, action) {
  //
  const { type, payload } = action;
  switch (type) {
    //
    case "":
      return state;

    default:
      return state;
  }
}
export default reducer;
